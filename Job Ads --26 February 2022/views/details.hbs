{{!-- 
    <main>
        <section id="details-info">
            <h1>Details</h1>
            <div class="coin-image">
                <img src="{{crypto.image}}">
            </div>

            <div class="coin-info">
                <div class="coin-text">
                    <h1 id="name">{{crypto.name}}</h1>
                    <h3 id="payment">Payment method: {{crypto.payment}}</h3>
                    <p id="price"><span>Price: ${{crypto.price}}</span></p>
                    <p id="description">{{crypto.description}}</p>
                </div>
                <div class="product-btn">

                    <!--Only for registered user and author of the publication -->
                    {{#if isAuthenticated}}
                        {{#if isOwner}}
                            <div class="author">
                                <a href="/{{crypto._id}}/edit" class="btn-edit">Edit</a>
                                <a href="/{{crypto._id}}/delete" class="btn-delete">Delete</a>
                            </div>
                        {{else}}
                            {{#if isBuyer}}
                                <!-- Logged in user who has already buy this crypto-->
                                <p class="buy-message">You already bought these crypto coins.</p>
                            {{else}}
                                <!-- Logged in user who has not yet buy this crypto-->
                                <a href="/{{crypto._id}}/buy" class="btn-buy">Buy</a>
                            {{/if}}
                        {{/if}}
                    {{/if}}

                </div>
            </div>
        </section>
    </main> --}}



{{!-- ************************************************************************************************************ --}}

        <main>

            <section id="details-page">
                <article class="details-gage-title">
                    <h1>Details Page</h1>
                </article>
                <article class="details-page-card">

                    <article class="details-page-card-image">
                        <!-- Do not forget to change the path to the image -->
                        <img src="/static/img/man.png" alt="details-image">
                    </article>

                    <article class="details-page-card-content">
                        <p>Author: {{whoIsOwner.email}}</p>
                        <h2>Headline: {{itemX.headline}}</h2>
                        <h3>Company name: {{itemX.companyName}}</h3>
                        <h4>Company description: {{itemX.companyDescription}}</h4>
                        <h5>Location: {{itemX.location}}</h5>

                    {{#if isAuthenticated}}
                        <article class="details-page-card-buttons">   
                            
                                    {{#if isOwner}}
                                        <!--only for registered users and author of the ad-->
                                        <a href="/{{itemX._id}}/edit" class="btn-edit">Edit</a>
                                        <a href="/{{itemX._id}}/delete" class="btn-delete">Delete</a>
                                    {{/if}}
                                    {{#unless isOwner}}
                                    {{#if isVoted}}
                                      <!--logged in user who has already applied for current ad-->
                                        <p class="already-applied">You have already applied!</p>
                                    {{else}}  
                                        <!--logged in user who did not apply for current ad-->
                                        <a href="/{{itemX._id}}/like" class="btn-apply">Apply now!</a>
                                        <p class="applied-people">Hurry up, {{itemX.votes.length}} people have already applied.</p>       
                                {{/if}}
                            {{/unless}}
                        {{/if}}
                        </article>
                    </article>

                </article>
            </section>

        {{#if isOwner}}
            <!--This section should only be visible to the author of the current ad-->
            <section id="info-about-candidates">
                <h2>Candidates for this ad:</h2>
                 {{#each itemX.votes}}
                    {{!-- <!\--If there are people who have applied, show each of them in the following format--\> --}}
                    <article class="info-about-candidates-card">
                        <h2>Email: {{email}}</h2>
                        <h3>Description of the candidate's skills: {{description}}</h3>
                    </article>
                {{/each}}
                {{!-- <!\--If not, you don't need to visualize anything further-\-> --}}
            </section>
        {{/if}}
</main>