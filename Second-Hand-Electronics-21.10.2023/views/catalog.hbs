    {{!-- <main>
        <section class="crypto-market">
            <h1><span>Crypto</span> Trade</h1>
            {{#if crypto}}
            <div class="offer-list">
                {{#each crypto}}
                <div class="coin">
                    <div class="coin-img">
                        <img src="{{image}}"/>
                    </div>
                    <div class="coin-info">
                        <h1>{{name}}</h1>
                        <p><span>Price: </span>${{price}}</p>
                        <p><span>Payment: </span>{{payment}}</p>
                    </div>

                    <a href="/{{_id}}/details" class="btn-details">Details</a>
                </div>
                {{/each}}
            </div>

            {{else}}
                <div class="no-offer">
                    <p>There are no crypto offers found!</p>
                </div>
            {{/if}}
        </section>
    </main> --}}




      <section id="viewCatalog" class="background-img">
            <!--If there are one or more electronics-->
            {{#if crypto}}
            <div class="added-electronics">
                {{#each crypto}}
                    <a href="/{{_id}}/details" class="added-electronics-in-market">
                        <img src={{image}} class="picture-added-electronics">
                        <h3>{{name}}</h3>
                        <span>Price: ${{price}}</span>
                    </a>
                {{/each}}
            </div>

            {{else}}
                <div class="guest">
                    There are no electronics found...
                </div> 
            {{/if}}
        </section>